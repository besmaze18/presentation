package com.fittrack.user.domain;

/** Provenance of a body measurement, so wearable-derived values stay distinguishable. */
public enum MeasurementSource {
    MANUAL,
    WHOOP,
    FUTURE_INTEGRATION
}
